const Navbar = ({ options, activeStep, showFull }) => {
  //TODO: Add conditional logic display of active step based on prop of shared state
  console.log(options);

  return (
    <div>
      <ul>{}</ul>{" "}
    </div>
  );
};
export default Navbar;
